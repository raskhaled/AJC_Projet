name: CI
on: [push]
jobs: 
  test_build_run_push:
    run-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run test
        run :
          pip install pytest
          pytest
          